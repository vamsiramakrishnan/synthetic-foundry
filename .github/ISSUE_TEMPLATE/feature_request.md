---
name: Feature request
about: A corpus you cannot express, a knob that does not exist, a vertical that is missing
labels: enhancement
---

## The corpus you were trying to make

<!-- Describe the enterprise or the dataset, not the implementation: "a
     hospital's quarter-end with a coding backlog", "five insurers whose tail
     length varies". If you tried a command and were refused, paste the
     refusal — refusals name what is missing on purpose. -->

## What you tried

```bash
# Commands, flags, pack/spec files — whatever got closest.
```

- **Version**: `worldloom version` →

## What would need to be true

<!-- What the corpus would contain that it cannot today. If you know which
     seam it belongs to (a pack, a facet, a physics range, an authored
     process, a new vertical), say so — AGENTS.md maps them. -->
